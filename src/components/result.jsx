const Result = ({ weight, gender, drinks, hours }) => {

  const calculateBAC = () => {
    const r = gender === "male" ? 0.68 : 0.55;

    let totalAlcohol = 0;
  }
   // drinks.forEach(drink => {
    return(<></>)
   }